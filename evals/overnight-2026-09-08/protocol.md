# Overnight transfer trial — fixed before execution

Start: 2026-09-08 01:08:32 UTC. Input commit: 2d7f372b694e7ed9ba5c65f37697b6950cf0ff85.
Scope: two product scenarios, two fresh authors each, identical brief and blank
Figma starting files within each pair. Standard installed Figma skills for all;
one author per pair also reads the pinned current LeinoDesign skill. No prior
trial outcomes, reviewer checklist, sibling artifacts or expected winner are
given to authors. Root knows allocation. Neutral 01/02 labels for user review.
No user preference can be inferred while the user is asleep.

Use at most three authors concurrently; start fourth when a slot is free.
Record dispatch/start/end per author and stage; elapsed includes tool latency,
browser use and packaging, not pure model speed. No cost estimates without
measured telemetry. Creation/preparation/root review are separate.

Freeze each initial artifact directory with SHA256 after author completion.
Give the exact equal change request below only after both initial pair outputs
are frozen. Do not provide design feedback during measured stages. Log any
procedural assistance or tool failures. Freeze revisions before root edits.

## Desktop change request — withheld initially

Rename navigation «Обращения» (or the corresponding existing queue section)
to «Очередь». Add a new priority «Критический». Change ONLY SD-1042 to this
priority and rename its subject to «Отчёт по всем филиалам за август не
формируется — требуется выгрузка до 12:00». Keep the saved assignee Елена Волкова,
status Новое and original description. The priority filter must now include
Высокий and Критический, clearly labelled; still two matching requests.
Keep the existing 1440 states and add a 1024-wide filtered queue with access to
the same details/assignment flow. Preserve the return context and other five
requests. Save before/after evidence and finish.

## Mobile change request — withheld initially

Rename ONLY «Запись подкаста» to «Запись подкаста с двумя микрофонами», change
its duration from 60 to 90 minutes and price from 1 800 ₽ to 2 400 ₽ throughout
the selection, review and success states. The other service remains unchanged.
The formerly selected 16:00 becomes unavailable before confirmation. Add the
state explaining this and a path to choose 18:00 while preserving the contacts
and comment. Review and success must reflect the replacement time. Keep 390 px
screens and add a 320-wide review screen with all essential details and actions.
Save before/after evidence and finish.

## Independent review

Read screenshots at meaningful scale and actual native node values. Evaluate
required content, task hierarchy, field/state semantics, text wrapping and
action reachability. Exercise main and recovery paths in Chrome player; distinguish
graph readback, isolated PNGs and actual browser observation. Desktop: open/close
from both queues, assign/cancel, preserve filtered context. Mobile: error/recovery,
review back-navigation, unavailable-slot replacement, confirmation consistency.
Verify displayed service/priority values after change, including untouched peers.

Perform a real local public-property edit in each file plus a compatible master
change, read back at least two consumers, restore all values and bindings and
compare final state to baseline. No source library ratio score. If a design
does not expose a sensible reusable boundary, report it rather than inventing
one to help the score. Preserve frozen author outputs before any root correction.

Newly added LeinoDesign rules under test: inner JSON completeness and real
prototype-player/context validation. Do not teach these to control authors.
Report self-corrections, root findings and root fixes separately; do not turn
author self-reports into independent proof. Only demonstrated general failures
justify narrow skill edits. Do not change skill files until all authors finish.

Deliver neutral comparisons, an evidence-backed report, usage examples and
known limits. Validate changed skill/scripts, commit and push owned work.
Stop after these two pairs and bounded corrections; no unattended recurring
campaign, publication, global install, production edits or invented user acceptance.
